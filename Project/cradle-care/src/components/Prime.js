import React from 'react';

export default function Prime() {
  return (
    <div
      style={{
        minHeight: '100vh',
        background: 'linear-gradient(135deg, #1a1a1a, #004d80)',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        color: '#ffffff',
        fontSize: '48px',
        fontWeight: 'bold',
        textTransform: 'uppercase',
        letterSpacing: '2px',
        padding: '40px',
        textAlign: 'center'
      }}

      id='prime'
    >
      Prime
    </div>
  );
}
